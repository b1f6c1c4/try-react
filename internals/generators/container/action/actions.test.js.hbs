  describe('{{ camelCase name }} action', () => {
    it('has a type of {{ constantCase name }}_ACTION', () => {
      const expected = {
        type: {{ constantCase name }}_ACTION,
      };
      expect({{ camelCase name }}Action()).toEqual(expected);
    });
  });

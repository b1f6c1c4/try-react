  it('should handle {{ camelCase name }} action', () => {
    const originalState = state;
    const expectedResult = state;

    expect({{ camelCase container }}Reducer(originalState, {{ camelCase name }}Action())).toEqual(expectedResult);
  });

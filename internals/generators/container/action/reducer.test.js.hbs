  it('should handle {{ camelCase actionName }} action', () => {
    const originalState = state;
    const expectedResult = state;

    expect({{ camelCase name }}Reducer(originalState, {{ camelCase actionName }}Action())).toEqual(expectedResult);
  });

  it('should handle {{ camelCase name }} request', () => {
    const originalState = state;
    const expectedResult = state;

    expect({{ camelCase container }}Reducer(originalState, {{ camelCase name }}Request())).toEqual(expectedResult);
  });

  it('should handle {{ camelCase name }} success', () => {
    const originalState = state;
    const result = { };
    const expectedResult = state;

    expect({{ camelCase container }}Reducer(originalState, {{ camelCase name }}Success(result))).toEqual(expectedResult);
  });

  it('should handle {{ camelCase name }} failure', () => {
    const originalState = state;
    const error = { };
    const expectedResult = state;

    expect({{ camelCase container }}Reducer(originalState, {{ camelCase name }}Failure(error))).toEqual(expectedResult);
  });

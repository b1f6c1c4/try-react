import { fromJS } from 'immutable';

import {
  {{ constantCase actionName }}_ACTION,
{{#if wantSagas}}
  {{ constantCase sagaName }}_REQUEST,
  {{ constantCase sagaName }}_SUCCESS,
  {{ constantCase sagaName }}_FAILURE,
{{/if}}
} from './constants';

const initialState = fromJS({
  {{ camelCase selectorName }}: 'the {{ camelCase selectorName }}',
});

function {{ camelCase name }}Reducer(state = initialState, action) {
  switch (action.type) {
    // Actions
    case {{ constantCase actionName }}_ACTION:
      return state;
{{#if wantSagas}}
    // Sagas
    case {{ constantCase sagaName }}_REQUEST:
      return state;
    case {{ constantCase sagaName }}_SUCCESS:
      return state;
    case {{ constantCase sagaName }}_FAILURE:
      return state;
{{/if}}
    // Default
    default:
      return state;
  }
}

export default {{ camelCase name }}Reducer;

describe('makeSelect{{ properCase name }}{{ properCase selectorName }}', () => {
  const select{{ properCase name }}{{ properCase selectorName }} = makeSelect{{ properCase name }}{{ properCase selectorName }}();

  it('should select {{ camelCase selectorName }}', () => {
    const {{ camelCase selectorName }} = 'value';
    const state = fromJS({
      {{ camelCase selectorName }},
    });
    const mockedState = fromJS({
      {{ camelCase name }}: state,
    });
    expect(select{{ properCase name }}{{ properCase selectorName }}(mockedState)).toEqual({{ camelCase selectorName }});
  });
});

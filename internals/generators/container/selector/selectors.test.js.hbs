describe('makeSelect{{ properCase container }}{{ properCase name }}', () => {
  const select{{ properCase container }}{{ properCase name }} = makeSelect{{ properCase container }}{{ properCase name }}();

  it('should select {{ camelCase name }}', () => {
    const {{ camelCase name }} = 'value';
    const state = fromJS({
      {{ camelCase name }},
    });
    const mockedState = fromJS({
      {{ camelCase container }}: state,
    });
    expect(select{{ properCase container }}{{ properCase name }}(mockedState)).toEqual({{ camelCase name }});
  });
});
